name: Publish to TestPyPI (develop)

on:
  push:
    branches: [develop]

permissions:
  contents: write
  pull-requests: write
  id-token: write
  pages: write

concurrency:
  group: publish-testpypi-develop
  cancel-in-progress: false

jobs:
  publish:
    name: Build & publish to TestPyPI
    runs-on: ubuntu-latest

    # Only run on merge commits (i.e., after a PR merge into develop).
    # This avoids publishing on direct pushes.
    if: startsWith(github.event.head_commit.message, 'Merge pull request')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build tooling
        run: |
          python -m pip install -U pip
          python -m pip install build twine

      - name: Build distributions
        run: python -m build

      - name: Publish to TestPyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.TEST_PYPI_API_TOKEN }}
        run: |
          python -m twine upload --repository-url https://test.pypi.org/legacy/ dist/*

  open_pr:
    name: Open PR develop -> release
    runs-on: ubuntu-latest
    needs: publish

    # Only after successful publish job (and only on merge commits)
    if: needs.publish.result == 'success'

    steps:
      - name: Create GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.PR_BOT_APP_ID }}
          private-key: ${{ secrets.PR_BOT_APP_PRIVATE_KEY }}

      - name: Create or update PR (develop -> release)
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;

            const base = 'release';
            const head = 'develop';

            const title = 'Release: merge develop into release';
            const body = [
              'Automated PR created after a successful publish to TestPyPI from develop.',
              '',
              'Merge this PR when you\'ve finished testing the TestPyPI build.'
            ].join('\n');

            // Look for an existing open PR from develop -> release
            const { data: pulls } = await github.rest.pulls.list({
              owner,
              repo,
              state: 'open',
              base,
              per_page: 100,
            });

            const existing = pulls.find(pr => pr.head && pr.head.ref === head);

            if (existing) {
              core.info(`PR already exists: #${existing.number} ${existing.html_url}`);
              // Keep title/body up to date (optional)
              await github.rest.pulls.update({
                owner,
                repo,
                pull_number: existing.number,
                title,
                body,
              });
              return;
            }

            // Create a new one. This uses GitHub's native compare of head vs base.
            try {
              const { data: pr } = await github.rest.pulls.create({
                owner,
                repo,
                title,
                head,
                base,
                body,
                maintainer_can_modify: true,
              });
              core.info(`Created PR: #${pr.number} ${pr.html_url}`);
            } catch (e) {
              // If there are no commits between head and base, GitHub returns 422.
              if (e.status === 422) {
                core.warning(`No PR created: '${head}' has no changes compared to '${base}'. (${e.message})`);
                return;
              }
              throw e;
            }
